# ZeroStack Backend - Full Dependencies
#
# This file includes ALL dependencies including ML/PyTorch.
# For faster installs, use one of these instead:
#
#   FAST (no ML):     pip install -r requirements-core.txt
#   FULL (with ML):   pip install -r requirements-ml.txt
#   TESTING:          pip install -r requirements-test.txt
#
# CPU-only PyTorch (saves ~1.5GB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-ml.txt

# Include ML dependencies (which includes core)
-r requirements-ml.txt

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-asyncio>=0.21.0
